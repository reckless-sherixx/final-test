export type Post = {
  id: string,
  title: string,
  description: string,
  coverImageUrl: string,
  content: string,
}

export type Activity = {
  id: string,
  type: string,
  title: string,
  description: string,
  coverImageUrl: string,
  content: string,
}